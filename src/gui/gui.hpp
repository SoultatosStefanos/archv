// Contains the root widget UI component of the GUI system.
// Soultatos Stefanos 2022

#ifndef GUI_GUI_HPP
#define GUI_GUI_HPP

// TODO Gui with config data, interactive from the config api and gui editor.
// NOTE: Only UI component because imgui changes are global.
// Facade class.
// Pseudo interface, must make call to global imgui with config data/api.
// Maybe implement as just free functions or singleton?
// If its implemented via functions, must provide ui wrapper.
// Probably singleton !!!!
// Maybe rename this to config ?
// Is this composed of the others? if yes do we need the manager?

#endif // GUI_GUI_HPP
